# Venues

Controls which venue types are loaded into the simulation, how they are read from CSV, and how capacity is tracked. Each named entry under `venue_types` defines one type of place that agents can be assigned to.

| File | Purpose |
|---|---|
| [`venues_config.yaml`](venues-config.md) | Venue type catalogue — enabled/disabled, CSV path, capacity schema, batch-mode loading |
